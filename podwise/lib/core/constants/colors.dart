import 'package:flutter/material.dart';

class AppColors {
  // 🌑 Backgrounds
  static const background = Color(0xFF0D0D0D);
  static const card = Color(0xFF1A1A1A);
  static const softCard = Color(0xFF222222);

  // 🌊 Primary (Teal Gradient Style)
  static const primary = Color(0xFF00C2A8);
  static const primaryDark = Color(0xFF008F7A);

  // 🔵 Secondary (Blue for buttons like "Get Started")
  static const secondary = Color(0xFF2D9CDB);

  // 📝 Text
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
  static const textMuted = Colors.white38;

  // ❤️ Accent / Icons
  static const accent = Color(0xFFFF5A7A);

  // ⚪ Borders
  static const border = Color(0xFF2A2A2A);

  // 🎧 Player Gradient (important for screen 3)
  static const playerGradientStart = Color(0xFF00C2A8);
  static const playerGradientEnd = Color(0xFF005F73);

  // 🟢 Active / Selected
  static const selectedBorder = Color(0xFF00E0C6);
}